services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: scry_test
      POSTGRES_PASSWORD: scry_test
      POSTGRES_DB: scry_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql
